---
import SwiperSlide from "./SwiperSlide.astro";

const wizards = await Astro.glob("../../content/wizard/*.{md,mdx}");
---

<section wizard>
  <p serif>
    <span blackletter>Amidst the tapestry of antiquity,</span> a dread fortress emerged,
    ensnared in the tendrils of mystic malevolence. Its ancient stones bore witness
    to the echoes of wickedness, whispering tales of shadows and desolation.
  </p>

  <div class="swiper wizards" fullscreen>
    <div class="swiper-wrapper">
      {
        wizards.map((post) => (
          <>
            <SwiperSlide
              imageURL={post.frontmatter.imageURL}
              title={post.frontmatter.title}
            />
          </>
        ))
      }
    </div>

    <div class="swiper-button-prev swiper-button"></div>
    <div class="swiper-button-next swiper-button"></div>
  </div>
</section>
